const createMatrix = n => {
  let subArrays = [];
  let nums = [];
	// create matrix subarray
	for (let i = 0; i < n; i++) {
    subArrays.push([]);
    nums.push(i+1);
    
  }
  console.log('running');
  console.log(nums)
	// create a an array of all numbers
};

createMatrix(8);
